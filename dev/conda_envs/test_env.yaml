name: test
channels:
  - rdkit
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  # Testing
  - pytest
  - pytest-cov
  - codecov
  #meta
  - typing
  - tqdm
  - ipywidgets
  #science
  - openforcefield = 0.8.3
  - pandas
  - numpy
  - sympy
  - scipy
  - matplotlib
  - rdkit
  
  # Pip-only installs
  - pip:
    - tables

